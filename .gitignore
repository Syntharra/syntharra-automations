node_modules/
.env
.env.local
*.log
.DS_Store

# Secrets / local-only (added 2026-04-09)
.claude/settings.local.json
docs/audits/2026-04-09-n8n-workflows-raw.json
docs/audits/n8n-backups-*/
retell-iac/snapshots/*_pre-*/agent.json

# Cold outreach lead lists — contain real business contact info, never commit
leads/

# Press release drafts — generated locally by tools/generate_press_release.py,
# not versioned so Dan can edit freely before sending to trade pubs.
press/

# retell-iac build artifacts (gitignored, regenerable from manifest)
build/

# Python bytecode — never commit
__pycache__/
*.pyc
*.pyo

# External plugin trees dropped into the workspace for reference.
# Not part of Syntharra; must never land in commits.
plugins/
everything-claude-code/
excalidraw-diagram-skill/
notebooklm/

# Screenshots and design artifacts — generated locally, not source of truth
*.png
*.jpg
*.jpeg

# Claude Code / tooling artifacts
.claude/scheduled_tasks.lock
.playwright-mcp/

# Raw automation-API dumps tend to contain inlined credentials (n8n HTTP
# nodes bake keys into headers). Save under docs/audits/ (also gitignored)
# or delete after use. See FAILURES.md 2026-04-09 prompt-compiler row.
*_raw.json
*.secrets.json
tests/fixtures/prompt_compiler/brp_input.json
tests/fixtures/prompt_compiler/_workflow_raw.json
.aidesigner/*
!.aidesigner/.gitkeep
