# Weekly Failure Analysis — 2026-04-13
_Auto-generated by Syntharra-Weekly-Failure-Analysis agent. Do not hand-edit — overwritten each Monday._

## Summary
- Total incidents tracked: 35
- Hotspot areas (3+ failures): n8n (12), general (10), retell (6), email (3), supabase (3)
- Unresolved incidents: 3

## Hotspots

### n8n — 12 failures
**Most recent 2 symptoms:**
1. Python `print()` of n8n workflow JSON crashed with `UnicodeEncodeError: 'charmap' codec can't encode character` (2026-04-10)
2. New clients saw sample/demo call data on their dashboard instead of an empty state because `.json()` threw on empty body (2026-04-10)

### general — 10 failures
**Most recent 2 symptoms:**
1. Saving the call forwarding guide via Chrome → Print → Save as PDF produced a light-background document — all dark gradient backgrounds and brand colors were lost (2026-04-10) ⚠️ UNRESOLVED
2. Multiple sections of the call forwarding PDF guide bled into each other when printed — Section 4 spilled into Section 5, Section 1 carrier QRs cut off (2026-04-10)

### retell — 6 failures
**Most recent 2 symptoms:**
1. Every execution of `4Hx7aRdzMl5N0uJP` completed with `status=error` — `client_subscriptions` never reached due to Telnyx credential throw + missing `onError` on downstream nodes (2026-04-10)
2. `POST /webhook/retell-calls` returned 0-byte body for agents with no call history, causing dashboard to load MOCK data (2026-04-10) ⚠️ UNRESOLVED (1 retell entry)

### email — 3 failures
**Most recent 2 symptoms:**
1. Live Transfer Number field was blank in the "Your AI Receptionist is Live" email (2026-04-08)
2. Premium clients never received the "Your AI Receptionist is Live" activation email — guard condition was blocking the send node (2026-04-08) ⚠️ P0 UNRESOLVED: Brevo API key still hardcoded in n8n workflow (2026-04-08)

### supabase — 3 failures
**Most recent 2 symptoms:**
1. `client_subscriptions` never written after any Jotform submission — reconcile node used PATCH on an empty table (silent 200 no-op) (2026-04-10)
2. First INSERT to `client_subscriptions` failed with CHECK constraint violation on `plan_type` — `'hvac_standard'` is not a valid value (2026-04-10)

---

## Unresolved Issues

| Date | Area | Symptom |
|---|---|---|
| 2026-04-10 | general | Saving call forwarding guide via Chrome Print → PDF lost all dark/colored backgrounds (resolved: "?" — CSS fix applied but user confirmation of "Background graphics" checkbox step may be outstanding) |
| 2026-04-10 | retell | 1 retell area entry flagged unresolved by analyze_failures.py — review the Pass 2 live-agent override and any open Retell issues |
| 2026-04-08 | email | Brevo API key hardcoded in n8n workflow — P0 security task, NOT YET FIXED. Key must be migrated to syntharra_vault |

---

## Skill File Gaps

### `skills/syntharra-infrastructure-SKILL.md` (n8n section)

**Gap 1 — formTrigger responseMode pattern not represented**
- FAILURES.md entry: 2026-04-09 — `https://n8n.syntharra.com/form/client-update` returned `{"code":0,"message":"Unused Respond to Webhook node found in the workflow"}`
- Rule covered: Rule 52 (RULES.md)
- Missing GOTCHA: formTrigger must use `responseMode: "responseNode"` with exactly one terminal `respondToWebhook` node. `"lastNode"` causes any explicit `respondToWebhook` to appear "unused." IF branches each having a `respondToWebhook` node are also rejected.
- **Auto-fixed this run:** YES — GOTCHA added

**Gap 2 — n8n public API DELETE is a hard delete (not archive)**
- FAILURES.md entry: 2026-04-09 — `DELETE /api/v1/workflows/{id}` hard-deleted `Premium Dispatcher — Google Calendar` with no recovery
- Rule covered: CLAUDE.md iron rules (but NOT in RULES.md as a numbered rule)
- Missing GOTCHA: Never call `DELETE /api/v1/workflows/{id}` on n8n public API — it is a permanent hard delete. Archive procedure: deactivate + rename with `[ARCHIVED-YYYY-MM-DD]` prefix.
- **Auto-fixed this run:** YES — GOTCHA added to skill; Rule 58 added to RULES.md

**Gap 3 — Supabase PATCH on empty table is a silent no-op**
- FAILURES.md entry: 2026-04-10 — Reconcile node used PATCH on `client_subscriptions` (zero rows) — 200 returned, nothing written
- Rule covered: Rule 35 (RULES.md)
- Missing GOTCHA: Supabase PATCH on 0 matched rows returns 200 silently. When a node is the creator (not updater) of a row, use POST. Verify table row count before pipeline design.
- **Auto-fixed this run:** YES — GOTCHA added

**Gap 4 — Check NOT NULL + CHECK constraints before any Supabase INSERT**
- FAILURES.md entry: 2026-04-10 — INSERT to `client_subscriptions` failed on CHECK constraint `plan_type IN ('standard', 'premium')` — `'hvac_standard'` rejected
- Rule covered: Rule 38 (RULES.md)
- Missing GOTCHA: `plan_type` on `client_subscriptions` accepts only `'standard'` or `'premium'`. Always query `information_schema.columns` + `pg_constraint` before any new INSERT.
- **Auto-fixed this run:** YES — GOTCHA added

---

### `skills/syntharra-retell-SKILL.md`

**Gap 5 — Phantom components block Auto Layout**
- FAILURES.md entry: 2026-04-08 — Retell canvas "Auto Layout" threw error due to blank inline component with placeholder text
- Rule covered: Rule 33 (RULES.md)
- Missing GOTCHA: After any flow edit session, inspect `components[]` for entries with placeholder text ("Describe what the AI should say or do") or zero node references. PATCH `{"components": []}` to clear safely.
- **Auto-fixed this run:** YES — GOTCHA added

**Gap 6 — prompt_compiler must validate edges and finetune node references**
- FAILURES.md entry: 2026-04-09 — `update_client_agent.py` PATCH rejected: "Please remove or update finetune examples before deleting the associated node or edge" — 3 edge/finetune bugs in prompt_compiler.py
- Rule covered: Rule 55 (RULES.md)
- Missing GOTCHA: Any script generating Retell flow JSON must assert every non-terminal node has a non-empty `edges` array, and every finetune example's `node_id` exists in the current node list.
- **Auto-fixed this run:** YES — GOTCHA added

---

### `skills/syntharra-email-SKILL.md`

**Gap 7 — No GOTCHA for email variable path tracing**
- FAILURES.md entries: 2026-04-08 — AI Phone Number blank; transfer_phone_number blank (both due to variable key drift across webhook → Set node → HTML template)
- Rules covered: Rule 23, Rule 24 (RULES.md)
- Missing GOTCHA: Always trace each email variable through the full path: webhook payload → Set node → Code node → email HTML. Key names drift silently at every intermediate hop.
- **Auto-fixed this run:** YES — GOTCHA added

**Gap 8 — No GOTCHA for verifying email send nodes fire after processor changes**
- FAILURES.md entry: 2026-04-08 — Premium "you're live" email never fired after a guard condition blocked it
- Rule covered: Rule 25 (RULES.md)
- Missing GOTCHA: After any call processor workflow change, do a full test run and confirm every email send node fires — guard conditions and branch fall-throughs can silently swallow emails.
- **Auto-fixed this run:** YES — GOTCHA added

---

## Rules Gaps

**Gap 1 — "Never DELETE on n8n public API" missing from RULES.md**
- FAILURES.md entry: 2026-04-09 — `DELETE /api/v1/workflows/{id}` hard-deleted `Premium Dispatcher — Google Calendar`
- Covered by: CLAUDE.md iron rules (added 2026-04-09)
- Missing from: `docs/RULES.md` (no numbered rule exists for this)
- **Auto-fixed this run:** YES — Rule 58 added to RULES.md

---

## Actions Taken This Run

1. Added GOTCHA to `skills/syntharra-infrastructure-SKILL.md`: formTrigger responseMode + respondToWebhook pattern
2. Added GOTCHA to `skills/syntharra-infrastructure-SKILL.md`: n8n public API DELETE is a hard delete — archive procedure
3. Added GOTCHA to `skills/syntharra-infrastructure-SKILL.md`: Supabase PATCH on empty table is a silent no-op — use POST when creating rows
4. Added GOTCHA to `skills/syntharra-infrastructure-SKILL.md`: Check NOT NULL + CHECK constraints before any Supabase INSERT
5. Added GOTCHA to `skills/syntharra-retell-SKILL.md`: Phantom components block Auto Layout — PATCH `{"components": []}` to clear
6. Added GOTCHA to `skills/syntharra-retell-SKILL.md`: prompt_compiler must validate all edges and finetune node references before PATCH
7. Added GOTCHA to `skills/syntharra-email-SKILL.md`: Always trace email variable path end-to-end (webhook → Set node → HTML)
8. Added GOTCHA to `skills/syntharra-email-SKILL.md`: Verify all email send nodes fire in sequence after any call processor change
9. Added Rule 58 to `docs/RULES.md`: Never DELETE on n8n public API — hard delete with no recovery

---

## Recommendations

1. **Resolve P0 immediately (next session):** Migrate the hardcoded Brevo API key in the n8n workflow to `syntharra_vault`. This is a live secret exposure risk. Use `tools/fetch_vault.py` + raw REST PUT pattern (Rule 11). Do not use n8n MCP (Rule 11). Log completion in FAILURES.md when done.

2. **Verify PDF print guide unresolved (next session):** Confirm with Dan that the "Background graphics" checkbox instruction was communicated and the PDF renders correctly. If confirmed, update FAILURES.md resolved field from "?" to "yes".

3. **Address n8n / Retell area accumulation:** n8n (12 failures) and retell (6) account for 51% of all incidents. Both areas would benefit from a pre-change checklist added to the relevant PLAYBOOKS.md sections — covering expression `=` prefix, `onError` settings, HTTP node vs Code node for outbound HTTP, and publish-after-every-update. This would reduce repeat-class failures.
