agent: premium
flow_template: flows/hvac-premium.template.json
# Lossless template: hvac-premium.template.json is a byte copy of the Premium TESTING
# snapshot (26 nodes). No component indirection — the full node bodies live in the template.
# Build = template with `excluded` nodes stripped + inbound edges cleaned.
components: []
overrides: {}
excluded:
  - spanish_routing_node
status: active_premium_testing_source
source_snapshot: snapshots/2026-04-06_premium-testing/flow.json
notes: |
  Premium MASTER does not yet exist in Retell. Current truth is Premium TESTING
  (agent_2cffe3d86d7e1990d08bea068f, flow conversation_flow_2ded0ed4f808).
  Spanish routing is excluded — Retell native multilingual handles Spanish callers inline.
  When Premium MASTER is created, register its agent_id in retell-iac/CLAUDE.md
  MASTERS table and in scripts/promote.py.
